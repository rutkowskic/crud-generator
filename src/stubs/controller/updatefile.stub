if ($request->hasFile('##name##')) {
    $data['##name##'] = $request->file('##name##')->store('##plural##');
    Storage::delete($##singular##->##name##);
} else {
    $data['##name##'] = $##singular##->##name##;
}